{
  "name": "haven",
  "version": "0.0.1",
  "private": true,
  "description": "Autonomous agent for the visually impaired",
  "workspaces": [
    "apps/*",
    "apps/backend/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "dependencies": {
    "uvicorn": "^0.0.1-security"
  }
}
